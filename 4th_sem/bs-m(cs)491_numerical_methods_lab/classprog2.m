%sort row-wise and column-wiseclc;rows=4;%columns=3;%step = 1;%for i = 1:step:rows%  for j = 1:step:columns%       matrix(i,j)=randi(40);      %  endfor%endfor  %disp("The Matrix is ");%disp(matrix)%disp("The Row-wise Column-Wise Sorted Matrix is ");%Approach: Following are the steps:%%1.Sort each row of the matrix.%2.Get transpose of the matrix.%3.Again sort each row of the matrix.%4.Again get transpose of the matrix.%x = matrix;x=randi([1,100],3,3)x=sort(x);x=x';x=sort(x);x=x';disp(x);clear all;